<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }

        a {
            color: inherit;
            text-decoration: none;
        }

        ul li {
            list-style: none;
        }

        button,
        input,
        textarea {
            font: inherit;
            border: none;
            background: none;
            outline: none;
        }

        body {
            color: #222;
            font: 400 17px/1.25 inherit;
        }

        .container {
            max-width: 1064px;
            padding: 20px;
            margin: auto;
        }

        .error {
            padding: 0.5rem 1rem;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 1rem;
            background: rgba(255, 0, 0, 0.3);
            color: red;
        }

        .nav {
            margin-bottom: 50px;
        }

        .nav-flex {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 2rem;
            font-weight: 500;
            letter-spacing: 2px;
        }

        .nav__list {
            display: flex;
        }

        .nav__list--item {
            margin-right: 20px;
        }

        .nav__list--item:last-child {
            margin-right: 0;
        }

        .nav__list--link {
            font-size: 1.5rem;
        }


        .form {
            width: 100%;
            height: 500px;
            padding: 20px 30px;
            margin: auto;
            border-radius: 5px;
            display: flex;
            flex-direction: column;
            background-color: #fafafa;
            box-shadow: 0px 0px 10px 1px #eee;
        }

        .form h1 {
            margin-bottom: 30px;
            font-size: 2rem;
            font-weight: 500;
            letter-spacing: 2px;
        }

        .form__item {
            margin-bottom: 20px;
        }

        .form__label {
            font-size: 1.3rem;
            margin-bottom: 5px;
        }

        .form__input {
            width: 100%;
            padding: 10px 20px;
            border-radius: 5px;
            background-color: #fff;
            border: 1px solid #c8c8c8;
            transition: all 0.1s ease-in-out;
        }

        .form__input:disabled {
            pointer-events: none;
            background-color: #e6e6e6;
        }

        .form__input:focus,
        .form__input:active {
            border-color: #646464;
            box-shadow: 0px 0px 10px 1px #eee;
        }

        .form__input:hover {
            box-shadow: 0px 0px 10px 1px #eee;
        }

        .form__controls {
            width: 100%;
            margin-top: auto;
            display: flex;
        }

        .form__controls .button {
            width: 50%;
            padding: 15px 0;
            text-align: center;
            cursor: pointer;
            font-size: 1.2rem;
            transition: all 0.1s ease-in-out;
        }

        .form__controls .button:first-child {
            border-top-left-radius: 5px;
            border-bottom-left-radius: 5px;
        }

        .form__controls .button:last-child {
            border-top-right-radius: 5px;
            border-bottom-right-radius: 5px;
        }

        .form__controls .primary {
            color: #000;
            background-color: #f0f0f0;
        }

        .form__controls .primary:hover {
            background-color: rgba(240, 240, 240, 0.3);
        }

        .form__controls .danger {
            color: #fff;
            background-color: #ff3737;
        }

        .form__controls .danger:hover {
            background-color: rgba(255, 55, 55, 0.9);
        }

        .add.form {
            height: auto;
        }

        .form__item {
            margin-bottom: 20px;
        }

        .form__label {
            font-size: 1.3rem;
            margin-bottom: 5px;
        }

        .form__input {
            width: 100%;
            padding: 10px 20px;
            border-radius: 5px;
            background-color: #fff;
            border: 1px solid #c8c8c8;
            transition: all 0.1s ease-in-out;
        }

        .form__input:focus,
        .form__input:active {
            border-color: #646464;
            box-shadow: 0px 0px 10px 1px #eee;
        }

        .form__input:hover {
            box-shadow: 0px 0px 10px 1px #eee;
        }

        .form__controls {
            width: 100%;
            margin-top: auto;
            display: flex;
        }

        .form__controls .button {
            width: 50%;
            padding: 15px 0;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1.2rem;
            transition: all 0.1s ease-in-out;
        }

        .form__controls .primary {
            color: #000;
            background-color: #e6e6e6;
        }

        .form__controls .primary:hover {
            background-color: rgba(230, 230, 230, 0.5);
        }

        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }

            .logo {
                font-size: 1.7rem;
                letter-spacing: 1.7px;
            }

            .nav__list--link {
                font-size: 1.3rem;
            }

            .form {
                padding: 10px 20px;
                height: 450px;
            }

            .form h1 {
                margin-bottom: 25px;
                font-size: 1.5rem;
            }

            .form__label {
                font-size: 1.1rem;
            }

            .form__input {
                padding: 5px 10px;
            }

            .form__item {
                margin-bottom: 15px;
            }

            .button {
                padding: 10px 0;
                font-size: 1.1rem;
            }
        }

        @media (max-width: 425px) {
            .container {
                padding: 10px;
            }

            .form {
                height: 400px;
                padding: 10px;
            }

            .form__controls {
                flex-direction: column;
            }

            .form__controls .button {
                width: 100%;
                font-size: 1rem;
            }

            .form__controls .button:first-child {
                border-radius: 0px;
                border-top-left-radius: 5px;
                border-top-right-radius: 5px;
            }

            .form__controls .button:last-child {
                border-radius: 0px;
                border-bottom-left-radius: 5px;
                border-bottom-right-radius: 5px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <form action="/auth/reset" method="POST" class="form">
            <h1>Восстановить пароль</h1>

            {{#if error}}
            <p class="error">{{error}}</p>
            {{/if}}

            <div class="form__item">
                <div class="form__label">Ваш email</div>
                <input type="email" name="email" class="form__input" />
            </div>

            <input type="hidden" name="_csrf" value="{{csurf}}">

            <div class="form__controls">
                <button class="button primary">Сбросить</button>
            </div>
        </form>
    </div>
</body>

</html>