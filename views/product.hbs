<div class="container">
  <div class="row justify-content-center">
    <div class="col col-5 text-center mt-3">
      <div class="card mb-3">
        <img src="{{product.img}}" class="card-img-top h-200" alt="{{product.title}}">
        <div class="card-body">
          <h5 class="card-title">{{product.title}}</h5>

          <p class="card-text"><small class="text-muted">{{product.price}}</small></p>
          <a href="/products" target="_blank" class="card-link">Вернуться к продукту</a>
        </div>
      </div>
    </div>
  </div>
</div>