<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>

    <style>
        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }

        a {
            color: inherit;
            text-decoration: none;
        }

        ul li {
            list-style: none;
        }

        button,
        input,
        textarea {
            font: inherit;
            border: none;
            background: none;
            outline: none;
        }

        body {
            color: #222;
            font: 400 17px/1.25 inherit;
        }

        .container {
            max-width: 1064px;
            padding: 20px;
            margin: auto;
        }

        .error {
            padding: 0.5rem 1rem;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 1rem;
            background: rgba(255, 0, 0, 0.3);
            color: red;
        }

        .nav {
            margin-bottom: 50px;
        }

        .nav-flex {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 2rem;
            font-weight: 500;
            letter-spacing: 2px;
        }

        .nav__list {
            display: flex;
        }

        .nav__list--item {
            margin-right: 20px;
        }

        .nav__list--item:last-child {
            margin-right: 0;
        }

        .nav__list--link {
            font-size: 1.5rem;
        }

        .nfTitle {
            white-space: nowrap;
            font-size: 3rem;
            font-weight: 500;
            letter-spacing: 2px;
        }

        .nfText {
            margin-bottom: 20px;

            font-size: 1.5rem;
        }

        .nfLink {
            color: blue;
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="nfTitle">Ошибка 404</h1>
        <p class="nfText">Страница не найдена</p>

        <a href="/" class="nfLink">Вернутсья на главную</a>
    </div>

</body>

</html>